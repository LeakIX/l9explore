# WIP

```sh 
Usage: l9explore service

Explores services

Flags:
  -h, --help                                  Show context-sensitive help.

  -s, --plugin-dir="~/.l9/plugins/service"
  -t, --max-threads=10                        Max threads
  -l, --only-leak                             Discards services events
  -x, --explore-timeout=3s
      --debug

```